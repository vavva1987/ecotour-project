import logo from '../../assets/brand-logo.png'

type Props = {}

const Logo = (props: Props) => {
    return (
        // <div>Logo</div>
        <img src={logo} alt="" />
    )
}

export default Logo
